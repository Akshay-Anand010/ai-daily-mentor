"use client";
import {useState} from "react";
const api = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";
export function Subscribe(){const [email,setEmail]=useState("");const [status,setStatus]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setStatus("Submitting…");try{const r=await fetch(`${api}/api/subscribe`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setStatus(r.ok?"You’re on the list—welcome.":"Please check the email address and try again.")}catch{setStatus("Subscription service is unavailable right now.")}}return <form onSubmit={submit} className="mt-8 flex max-w-lg flex-col gap-3 sm:flex-row"><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="rounded-lg border border-slate-600 bg-slate-900 px-4 py-3"/><button className="rounded-lg bg-cyan-400 px-5 py-3 font-semibold text-slate-950">Get daily lessons</button><span aria-live="polite" className="text-sm text-slate-400">{status}</span></form>}
