import { Subscribe } from "../components/subscribe";
const api = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";
async function getLesson(){try{const r=await fetch(`${api}/api/lessons/today`,{cache:"no-store"});return r.ok?await r.json():null}catch{return null}}
export default async function Home(){const lesson=await getLesson();return <><section className="rounded-3xl border border-cyan-400/20 bg-gradient-to-br from-cyan-400/10 to-violet-500/10 p-8 md:p-14"><p className="font-medium text-cyan-300">A focused 15-minute habit</p><h1 className="mt-3 max-w-3xl text-4xl font-bold tracking-tight md:text-6xl">Learn the AI concepts that matter—one clear lesson a day.</h1><p className="mt-5 max-w-2xl text-lg text-slate-300">Expert synthesis, practical code, architecture reasoning, and a quiz—delivered to your inbox every morning.</p><Subscribe /></section>{lesson&&<section className="mt-10 rounded-2xl border border-slate-800 bg-slate-900/50 p-7"><p className="text-sm text-cyan-300">TODAY • {lesson.topic}</p><h2 className="mt-2 text-2xl font-semibold">{lesson.title}</h2><article className="prose prose-invert mt-5 max-w-none whitespace-pre-wrap text-slate-300">{lesson.content}</article><a className="mt-6 inline-block rounded-lg border border-slate-600 px-4 py-2" href={`${api}/api/lessons/${lesson.id}/pdf`}>Download PDF</a></section>}</>}
