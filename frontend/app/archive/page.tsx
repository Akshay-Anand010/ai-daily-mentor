const api=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
async function getLessons(){try{return await (await fetch(`${api}/api/lessons`,{cache:"no-store"})).json()}catch{return []}}
export default async function Archive(){const lessons=await getLessons();return <><h1 className="text-3xl font-bold">Lesson archive</h1><p className="mt-2 text-slate-400">Searchable history of your AI learning journey.</p><div className="mt-7 grid gap-4">{lessons.map((l:any)=><article key={l.id} className="rounded-xl border border-slate-800 p-5"><p className="text-sm text-cyan-300">{l.topic} · {l.difficulty}</p><h2 className="mt-1 text-xl font-semibold">{l.title}</h2><a className="mt-3 inline-block text-sm text-cyan-300" href={`${api}/api/lessons/${l.id}/pdf`}>Download PDF →</a></article>)}{!lessons.length&&<p className="mt-8 text-slate-400">Your first lesson will appear here after the service starts.</p>}</div></>}
